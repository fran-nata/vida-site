---
import { site } from '../lib/site';
interface Props {
  title?: string;
  description?: string;
}
const {
  title = 'Obtenez votre estimation gratuite',
  description = 'Réponse rapide, sans engagement.',
} = Astro.props;
---
<section id="contact" class="bg-navy px-8 py-18 text-center">
  <div class="max-w-[640px] mx-auto">
    <p class="text-[13px] font-bold tracking-wide uppercase text-accent-light mb-3">Passez à l'action</p>
    <h2 class="font-heading text-[30px] font-bold text-white mb-4">{title}</h2>
    <p class="text-base leading-relaxed text-footer-muted mb-7">{description}</p>
    <div class="flex gap-3.5 justify-center flex-wrap">
      <a href="/#estimateur" class="inline-block bg-accent text-white px-7 py-3.5 rounded-[10px] text-[15px] font-bold">Obtenir mon estimation &amp; réserver →</a>
      <a href={site.phoneHref} class="inline-block bg-white text-navy px-7 py-3.5 rounded-[10px] text-[15px] font-bold">Appeler {site.phone}</a>
    </div>
  </div>
</section>
